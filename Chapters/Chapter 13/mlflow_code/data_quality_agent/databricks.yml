# This is a Databricks asset bundle definition for data_quality_agent.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: data_quality_agent
  uuid: d08bc209-3b2b-4356-b9eb-fc5eead4a4ca

include:
  - resources/*.yml
  - resources/*/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405613581513114.14.azuredatabricks.net
    variables:
      catalog: models_and_more
      schema: dev
  prod:
    mode: production
    workspace:
      host: https://adb-7405613581513114.14.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/alan.dennis@covasant.com to make sure we only have a single copy.
      root_path: /Workspace/Users/alan.dennis@covasant.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: models_and_more
      schema: prod
    permissions:
      - user_name: alan.dennis@covasant.com
        level: CAN_MANAGE
